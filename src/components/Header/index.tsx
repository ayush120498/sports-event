import './style.scss';

const Header = (): JSX.Element => (
  <header>
    <div className="header" data-testid="header-test">
      <h2>Sports Day</h2>
    </div>
  </header>
);

export default Header;
